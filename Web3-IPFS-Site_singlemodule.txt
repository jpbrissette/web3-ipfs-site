// Web3 IPFS-Compatible Site using React, HashRouter, Tailwind CSS

// main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { HashRouter } from 'react-router-dom';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <HashRouter>
    <App />
  </HashRouter>
);

// App.jsx
import { Routes, Route, Link } from 'react-router-dom';
import Home from './pages/Home';
import About from './pages/About';

export default function App() {
  return (
    <div className="min-h-screen bg-gray-900 text-white">
      <nav className="p-4 bg-gray-800 flex justify-between">
        <Link to="/" className="font-bold">Web3 IPFS Site</Link>
        <div className="space-x-4">
          <Link to="/">Home</Link>
          <Link to="/about">About</Link>
        </div>
      </nav>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </div>
  );
}

// pages/Home.jsx
export default function Home() {
  return (
    <div className="p-8">
      <h1 className="text-4xl font-bold mb-4">Welcome to Web3!</h1>
      <p>This site is fully hosted on IPFS via Fleek.</p>
    </div>
  );
}

// pages/About.jsx
export default function About() {
  return (
    <div className="p-8">
      <h1 className="text-4xl font-bold mb-4">About This Site</h1>
      <p>Built with React + Tailwind CSS. Deployed on Fleek using IPFS.</p>
    </div>
  );
}

// index.css
@tailwind base;
@tailwind components;
@tailwind utilities;

// tailwind.config.js
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}"
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

// index.html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Web3 IPFS Site</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>

// Instructions for deploying on Fleek
// 1. Push this code to a GitHub repository.
// 2. Sign in to https://fleek.co and connect your GitHub.
// 3. Create a new site from the repo.
// 4. Use build command: `npm run build` and publish directory: `dist/`
// 5. Fleek handles IPFS deployment automatically.
